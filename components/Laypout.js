// 1. 유지보수
// 2. git --> mySever -->

import { todoAPI } from './api';

//3. 모듈화가 먼지 모르는게 보여요

const Layout = () => {
  const addTodo = (todo) => {
    todoAPI.addTodo();
  };

  return <div onClick={() => addTodo({ title: 'heesung' })}></div>;
};

const Layout = () => {
  const addTodo = (todo) => {
    todoAPI.addTodo();
  };

  return <div onClick={() => addTodo({ title: 'heesung' })}></div>;
};

const Layout = () => {
  const addTodo = (todo) => {
    todoAPI.addTodo();
  };

  return <div onClick={() => addTodo({ title: 'heesung' })}></div>;
};

const Layout = () => {
  const addTodo = (todo) => {
    todoAPI.addTodo();
  };

  return <div onClick={() => addTodo({ title: 'heesung' })}></div>;
};

const Layout = () => {
  const addTodo = (todo) => {
    todoAPI.addTodo();
  };

  return <div onClick={() => addTodo({ title: 'heesung' })}></div>;
};

const Layout = () => {
  const addTodo = (todo) => {
    todoAPI.addTodo();
  };

  return <div onClick={() => addTodo({ title: 'heesung' })}></div>;
};

const Layout = () => {
  const addTodo = (todo) => {
    todoAPI.addTodo();
  };

  return <div onClick={() => addTodo({ title: 'heesung' })}></div>;
};

const Layout = () => {
  const addTodo = (todo) => {
    todoAPI.addTodo();
  };

  return <div onClick={() => addTodo({ title: 'heesung' })}></div>;
};

const Layout = () => {
  const addTodo = (todo) => {
    todoAPI.addTodo();
  };

  return <div onClick={() => addTodo({ title: 'heesung' })}></div>;
};
