export type ThemeType = typeof light;

export const light = {
  primary: 'black',
  text: '#000',
  background: '#fff',
};
export const dark: ThemeType = {
  primary: 'black',
  text: '#000',
  background: '#fff',
};

const theme = light;
export default theme;
